apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "strfry-chart.fullname" . }}-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
